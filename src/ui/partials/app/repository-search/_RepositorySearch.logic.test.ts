// import { RepositorySearchPartialLogic } from './_RepositorySearch.logic';

describe('RepositorySearchPartialLogic', () => {

    // beforeAll(() => {
    //     jest.useFakeTimers();
    //     jest.spyOn(global, 'setTimeout');
    // });
    // afterAll(() => {
    //     jest.useRealTimers();
    //     jest.clearAllTimers();
    // });
    it('should test RepositorySearchPartialLogic', () => {

        // expect(true).toBe(true);

        // const spy = jest
        //     .spyOn(RepositorySearchPartialLogic, RepositorySearchPartialLogic.test.name as keyof typeof RepositorySearchPartialLogic);
        //     .mockReturnValue(123);
        // expect(spy).toBeCalledTimes(1);

        // const mockedRepositorySearchPartialLogic = jest.mocked(RepositorySearchPartialLogic);
        // mockedRepositorySearchPartialLogic.test.mockReturnValue(456);

    });
});
