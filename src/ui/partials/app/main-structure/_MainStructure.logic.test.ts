// import { MainStructurePartialLogic } from './_MainStructure.logic';

describe('MainStructurePartialLogic', () => {

    // beforeAll(() => {
    //     jest.useFakeTimers();
    //     jest.spyOn(global, 'setTimeout');
    // });
    // afterAll(() => {
    //     jest.useRealTimers();
    //     jest.clearAllTimers();
    // });
    it('should test MainStructurePartialLogic', () => {

        // expect(true).toBe(true);

        // const spy = jest
        //     .spyOn(MainStructurePartialLogic, MainStructurePartialLogic.test.name as keyof typeof MainStructurePartialLogic);
        //     .mockReturnValue(123);
        // expect(spy).toBeCalledTimes(1);

        // const mockedMainStructurePartialLogic = jest.mocked(MainStructurePartialLogic);
        // mockedMainStructurePartialLogic.test.mockReturnValue(456);

    });
});
